#include "main.h"
/**
 * print_last_digit - nmmshjjj
 * @i: ghdhggdhh
 * Description: hhsydgfdg
 * Return: 0
 */
int print_last_digit(int i)
{
int num;
if (i >= 0)
num = _putchar((i % 10) + '0');
else
num = _putchar((-i % 10) + '0');
return (num);
}
