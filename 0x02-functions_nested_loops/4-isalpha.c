#include <ctype.h>
#include "main.h"
/**
 * _isalpha.c - is alphabet
 * @c: values
 * Description: vavav
 * Return: 1 or 1
 */
int _isalpha(int c)
{
if (isalpha(c) > 0)
{
return (1);
else
return (0);
}

